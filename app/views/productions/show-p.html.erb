<div class="container-fluid order-custumer">
  <div class="row">
    <div class="  col-sm-10 col-sm-offset-1 col-md-10 col-md-offset-2 col-lg-10 col-lg-offset-2">

      <!-- modal -->
      	<div id="imageModal" class="modal fade modal-style" role="dialog ">
      				<div class="modal-dialog">
      					<div class="modal-content">
      					</div>
      				</div>
      		</div>
      <!--  modal-->

      <!-- modal passwords_resets-->
      	<div id="imageModal1" class="modal fade modal-style" role="dialog ">
      				<div class="modal-dialog">
      					<div class="modal-content">
      					</div>
      				</div>
      		</div>
      <!--  modal passwords_resets-->

    </p><b>User name:</b><%= @customer.full_name %></p>

      <% @customer.orders.each do |order| %>
        <p>
          <b>Order number:</b><%= order.id %>
        </p>

        <p>
          <b>Requested in:</b> <%= order.created_at.strftime("%b %d %Y %H:%M ") %>
        </p>

        <p><b>Acquired in:</b> <%= order.store %></p>
        <br>
        <p><b>Candies:</b>
         <% order.products.each do |p| %>
         <ul>
              <li><%= p.title %></li>
            </ul>
            <% end %>
        </p>

      <% end %>

    </div>
  </div>
</div>
